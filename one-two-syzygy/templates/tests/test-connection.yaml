apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "one-two-syzygy.fullname" . }}-test-connection"
  labels:
{{ include "one-two-syzygy.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "one-two-syzygy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
