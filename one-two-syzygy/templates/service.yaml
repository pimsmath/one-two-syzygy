apiVersion: v1
kind: Service
metadata:
  name: {{ include "one-two-syzygy.fullname" . }}
  labels:
{{ include "one-two-syzygy.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "one-two-syzygy.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
